            /* else if((int)userInput[i - 1] == 32) // Checking if prev char is a space
                if(((int)userInput[i + 1] >= 33 && (int)userInput[i + 1] <= 57) || (int)userInput[i + 1] == 32) //Checking if next is a digit or a space
                    userInput[i] = '#';
                else
                    continue; */



                    /* 
        if((int)userInput[i] > 57)
        {
            tracker = 1;
            continue;
        }

        else if((int)userInput[i] == 32)
        {
            tracker = 0;
            continue;
        }
        
        while (tracker >= 1) */